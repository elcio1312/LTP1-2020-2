#Operadores de divisao (/) e o resto da divisão (%)
divisão = 15 / 2
resto = 15 % 2

print("Divisão", divisão)
print("resto", resto)

#Ler um número para ver se ele é par ou impar
numero = int(input("Informe um número:"))
resto = numero % 2
#Olha para o valor do resto
if resto == 0:
  print(numero, "é par!")
else:
  print(numero, "é impar!")
