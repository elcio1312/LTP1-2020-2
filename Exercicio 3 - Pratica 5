a = float(input("Coeficiente A:"))
b = float(input("Coeficiente B:"))
c = float(input("Coeficiente C:"))

#Potencia no python- x elevado a y == x**y
delta = (b**2) - (4*a*c)

#Determina o número de raiz em função do valor de delta
if delta < 0:
 print("Não possui raízes reais")
elif delta > 0:
 x1 = (-b + delta**0.5) / (2*a)
 x2 = (-b - delta**0.5) / (2*a)
 print("Raízes:", x1, x2)
else:
  x2 = (-b - delta**0.5) / (2*a)
  print("Raiz", x2)
